import { getMetadata } from "@/_utils/getMetadata";

export const metadata = getMetadata(
  "Register",
  "Create an account to access all the features and services of our website."
);

function page() {
  return <div>register</div>;
}

export default page;
