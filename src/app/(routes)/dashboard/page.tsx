import { getMetadata } from "@/_utils/getMetadata"

export const metadata = getMetadata(
  "Dashboard",
  "Admin Dashboard: Manage and monitor all aspects of your platform, including user activity, orders, inventory, and settings. Easily access key data and control site features in one place."
);
function page() {

 
  return (
    <div>
      dashboard
    </div>
  )
}

export default page
